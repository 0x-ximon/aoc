import kotlin.test.Test
import kotlin.test.assertEquals

class TestMain {
    @Test
    fun test_first() {
        assertEquals(first(), 0)
    }

    @Test
    fun test_second() {
        assertEquals(second(), 0)
    }
}
